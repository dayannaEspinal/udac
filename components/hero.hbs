<section class="hero-panel" id="inicio">
    <style="background-image: url('src/bosquedanli1.jpg');">
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <div class="hero-overlay">
        <div class="hero-content">
            <h2>{{heroTitle}}</h2>
            <p>{{heroText}}</p>
            <a href="{{heroLink}}" class="btn-primary">{{heroCta}}</a>
        </div>
    </div>
</section>